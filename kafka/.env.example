# Kafka 외부 환경 설정 예시

# Kafka 서버 IP (실제 서버 IP로 변경)
KAFKA_SERVER_IP=192.168.1.100

# NiFi 서버 IP (방화벽 설정용)
NIFI_SERVER_IP=192.168.1.200

# Kafka 외부 접속 포트
KAFKA_EXTERNAL_PORT=9092

# 보안 설정 (선택사항)
# KAFKA_SECURITY_PROTOCOL=SASL_SSL
# KAFKA_SASL_MECHANISM=PLAIN